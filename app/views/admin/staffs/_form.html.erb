<%= simple_form_for([:admin, @staff], html: {multipart: true}) do |f| %>
  <%= f.input :name %>
  <%= f.input :biog %> 
  <%= f.input :role %> 
  <%= f.input :email %> 
  <%= f.input :phone %> 
  <%= f.file_field :image %><br />
  <%= f.submit class: 'btn btn-primary' %>
<% end %>