<%= form_for [:admin, @artist] do |f| %>
  <%= f.cropbox :photo %>
  <%= f.previewbox :photo %>
  <%= f.submit 'Crop' %>
<% end %>
 
<% content_for :extra_footer do %>
  <%= javascript_include_tag 'artists' %>
<% end %>